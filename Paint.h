#pragma once

#define BASIC_WIDTH 800
#define BASIC_HEIGHT 600
#define POSITIONS 2

// Function prototypes

bool closeButton(sf::Event);

bool leftCLickIsPressed(sf::Event event);

//void draw(sf::Vector2f lastPosition, sf::RenderWindow window, sf::RenderTexture canvasTexture);